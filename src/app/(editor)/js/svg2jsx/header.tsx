      {/* <header className="mt-4 p-4 flex space-x-2">
        <label className="block mb-2">
          <input
            type="checkbox"
            checked={settings.cleanLinksFromSVG}
            onChange={(e) =>
              setSettings((prev) => ({
                ...prev,
                cleanLinksFromSVG: e.target.checked,
              }))
            }
          />
          Remove links from SVG
        </label>
        <label className="block mb-2">
          <input
            type="checkbox"
            checked={settings.cleanClassesFromSVG}
            onChange={(e) =>
              setSettings((prev) => ({
                ...prev,
                cleanClassesFromSVG: e.target.checked,
              }))
            }
          />
          Remove classes from SVG
        </label>
        <label className="block mb-2">
          <input
            type="checkbox"
            checked={settings.cleanFillProperties}
            onChange={(e) =>
              setSettings((prev) => ({
                ...prev,
                cleanFillProperties: e.target.checked,
              }))
            }
          />
          Remove fill properties from SVG
        </label>
        <label className="block mb-2">
          <input
            type="checkbox"
            checked={settings.clearColors}
            onChange={(e) =>
              setSettings((prev) => ({
                ...prev,
                clearColors: e.target.checked,
              }))
            }
          />
          Clear colors (stroke, fill, etc.)
        </label>
        <label className="block mb-2">
          <input
            type="checkbox"
            checked={settings.clearDimensions}
            onChange={(e) =>
              setSettings((prev) => ({
                ...prev,
                clearDimensions: e.target.checked,
              }))
            }
          />
          Clear width/height
        </label>
        <label className="block mb-2">
          <input
            type="checkbox"
            checked={settings.createReactComponent}
            onChange={(e) =>
              setSettings((prev) => ({
                ...prev,
                createReactComponent: e.target.checked,
              }))
            }
          />
          Create React Component
        </label>
        <label className="block mb-2">
          <input
            type="checkbox"
            checked={settings.pureSVGCopy}
            onChange={(e) =>
              setSettings((prev) => ({
                ...prev,
                pureSVGCopy: e.target.checked,
              }))
            }
          />
          Pure SVG Copy
        </label>
      </header> */}